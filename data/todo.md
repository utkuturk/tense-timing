- Pipeline: csv -> find recording names -> get subj names -> create a folder -> create a list where you match each folder with the .zip files -> unzip those files inside to those folders -> append the subj name to the file names.

- Figure out which ones are primed and which ones are burned and unprimed.

- Check the data.
